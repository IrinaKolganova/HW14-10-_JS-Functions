function getSum (){ 
    let a=prompt ('Назовите a');
    let b=prompt ('Назовите b');
    let sum=+a + +b
    alert ('a+b= '+sum );}
    
    function getDiff (){ 
        let c=prompt ('Назовите c');
        let d=prompt ('Назовите d');
        let diff=c - d
        alert ('c-d= '+diff );}

        function getMult (){ 
            let x=prompt ('Назовите x');
            let y=prompt ('Назовите y');
            let mult=x * y
            alert ('x*y= '+mult );}

            function getDiv (){ 
                let z=prompt ('Назовите z');
                let v=prompt ('Назовите v');
                let div=z / v
                alert ('z/v= '+div );}
